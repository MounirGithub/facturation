CREATE TABLE IF NOT EXISTS CLIENT (
    referenceClient VARCHAR(20) PRIMARY KEY,
    civilite VARCHAR(50),
    nom VARCHAR(50),
    prenom VARCHAR(50),
    siret VARCHAR(50),
    ca VARCHAR(50),
    raisonSociale VARCHAR(50)
);
